<?php

/**
 * Implements hook_install().
 */
function respondrupal_install() {
  theme_enable(array('responsive', 'rubik'));
  variable_set('theme_default', 'responsive');
  variable_set('admin_theme', 'rubik');
  
}
